#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("BYTES para FLOAT: %i\n", sizeof(float));
    printf("BYTES para DOUBLE: %i\n", sizeof(double));
    printf("BYTES para LONG DOUBLE: %i\n", sizeof(long double));
    return 0;
}
