#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("BYTES para CHAR: %i\n", sizeof(char));
    printf("BYTES para SHORT INT: %i\n", sizeof(short int));
    printf("BYTES para INT: %i\n", sizeof(int));
    printf("BYTES para LONG INT: %i\n", sizeof(long int));
    printf("BYTES para LONG LONG INT: %i\n", sizeof(long long int));
    return 0;
}
